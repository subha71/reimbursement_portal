<h2>Companies</h2>
<%= link_to 'Add New Company', new_company_path, class: 'btn btn-primary mb-3' %>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Employee Count</th>
      <th>Total Reimbursements</th>
    </tr>
  </thead>
  <tbody>
    <% @companies.each do |company| %>
      <tr>
        <td><%= company.name %></td>
        <td><%= company.employee_count %></td>
        <td><%= number_to_currency(company.reimbursement_total, unit: "₹") %></td>
      </tr>
    <% end %>
  </tbody>
</table>
